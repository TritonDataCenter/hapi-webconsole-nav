enum Continent {
  AFRICA
  ASIA
  EUROPE
  NORTH_AMERICA
  SOUTH_AMERICA
  AUSTRALIA
}

type Region {
  name: String!
  continent: Continent!
  datacenters: [Datacenter]!
}

type Datacenter {
  name: String!
  url: String!
}

type Category {
  name: String!
  slug: String
  services: [Service]!
}

type Service {
  name: String!
  slug: String!
  description: String!
  url: String
  tags: [String]
}

type Query {
  regions: [Region]
  categories: [Category]
  service(slug: String): Service
}